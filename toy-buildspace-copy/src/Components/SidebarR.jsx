import React from "react";

const SidebarR = ({size}) => {
    return (
        <div className='sticker' style={{width: `${size}%`, backgroundColor: 'aqua', height: '100vh', marginLeft: '80%', position: 'absolute'}}>adsf</div>
    )
}

export default SidebarR
